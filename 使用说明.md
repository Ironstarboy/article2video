# 理论文章转视频 · 使用说明

> 站点地址:**http://8.130.213.80:20013/ttv/**(外网直接访问,无需登录)
> 服务器部署目录:`/mnt/workspace/ttv/`

## 一、使用流程

0. **入口页(项目历史)**:打开站点先看到之前做过的项目,按最近更新倒序。每条显示项目标题、视频类型与目标时长、当前状态/阶段进度,以及「分析脚本 → 构建预览 → 渲染成片 → 播报视频」四个步骤胶囊(已完成/失败的时间就写在上面)。**点任意一个步骤,直接回到那一步的结果**(分析面板 / Studio 编辑器 / 成片播放与下载 / 数字人播报播放器)并高亮该区域;有任务正在跑时列表每几秒自动刷新,不需要守着。标题由**大模型在分析完成后自动总结**,也可以在卡片或详情页「重命名」就地改成你认得的名字(改过之后自动标题不再覆盖它);完成的项目卡片上直接有「下载成片 / 播报视频」入口,不用点进去
1. **新建项目**:点「＋ 新建视频」→ **上传文章**(txt / md / docx,≤20MB)或**粘贴文字**(≥50 字)→ 选风格(三套预设或四维度自由组合)→ 选**视频类型**(宣传/讲解,共用一个时长滑杆)→ 拖时长(宣传 30 秒 – 10 分钟;讲解 5 – 30 分钟,每 5 分钟一档)→「开始分析」
2. 服务器调用本地 DeepSeek-V4-Flash(vLLM @20001)分析:
   - **宣传视频**:两阶段分析——阶段一「分析」诊断核心论点、论证链节拍、数据清单(逐字核验)、金句清单(逐字)与帧计划,阶段二「脚本」按帧计划生成逐帧脚本
   - **讲解视频**:两步分析——先诊断文章类型与讲解方案(章节安排、逐段讲解要点、待批注句、可迁移写法、答题方法总结),再按章节**并行**分段生成逐帧脚本(耗时 3–10 分钟,页面显示阶段进度)
3. 二级页面查看**分析结果**(讲解视频含完整备课方案)与**逐帧脚本表**(可手动编辑或一句话让 AI 修改)→ 接着出片,**两条路任选**(见下两步);分析失败无脚本时可点「**重新分析**」重跑
4. **一键出片(推荐)**:点「**一键出片**」一次跑完「构建 → 渲染」—— 配音合成(CosyVoice3 三实例并行,自然语速)→ 旁白量不足目标时**自动二次拓展内容** → 组装 HyperFrames 项目 → 1080p 渲染(约 5–20 分钟,视时长;30 分钟讲解片更久,超时上限 3 小时)→ 完成后同一位置切换为成片播放,可在线观看,**下载按钮显示格式 + 文件大小**。这条路**不启动 Studio 编辑器、也不用中途再点一次**;项目列表卡片上也有这个按钮,不必进详情页。不需要的任务可用「**删除任务**」按钮清理(rendered/failed 可删)
5. **想先调时间线就走两步**:「构建预览」组装工程,预览就绪时 **Studio 编辑器已同步启动**,页面直接进入编辑器(时间线、逐帧编辑、实时预览)→ 满意后点「**渲染成片**」
6. **每一步都留档**:分析 / 构建 / 渲染 / 播报各阶段完成或失败都会记进项目历史(详情页「历史记录」面板逐条显示时间与结果摘要,渲染/播报可直接下载)。中途关掉页面、服务重启都不影响已出的结果,回来从入口页点对应步骤即可

**时长滑杆原理**:选择时长 → 分析引擎按档位规划帧数与旁白量(语速按 4.2 字/秒实测换算,配音始终自然语速 1.0)→ 构建时按真实配音时长向目标靠拢。**时长靠内容充实度填**:每帧旁白 2-4 句、画面元素填满上限,不靠降速或拖延。

## 二、视频类型

### 宣传视频(默认)
提炼主旨、归档宣传:把文章改写为庄重的政论宣传片(开场钩子 → 核心论点 → 是什么-为什么-怎么办 → 收束署名)。

### 讲解视频
**老师逐段精讲**,像讲作文一样拆解文章(申论讲解/政论解读式授课),不是把文章念一遍:

- 旁白为老师口吻:先点出这段在干什么、为什么这么写,再带学生看关键句;明确区分「文章认为……」与「目前已知事实是……」
- 三种讲解专用画面:
  - **原文段页(textblock)**:讲义式展示原文段落 + 本段作用标签
  - **逐句批注页(annotation)**:原句逐字展示 + 论点/论据/分析/对策/过渡/金句彩色标注 + 老师批注
  - **写法提炼页(method)**:把这段写法抽象成可迁移的板书卡片
- 复用宣传帧:statement(设问引导)/points/process(文章框架图)/contrast(原文观点 vs 已知事实)/quote(金句赏析)/data
- **原文引用逐字校验**:textblock/annotation 引用的原句必须逐字摘自原文(允许截断),校验门硬拦截改写与编造
- 时长 5–30 分钟,每 5 分钟一档;复用同一套四维风格系统

## 三、三套风格

| 键 | 名称 | 适用 |
|---|---|---|
| solemn-red | 庄重肃穆·中国红 | 人民日报评论、时政理论 |
| academic-ink | 清雅学术·墨黛青 | 马院论文、学术理论 |
| modern-blue | 现代锐意·科技蓝 | 改革、科技、新质生产力 |

详细样式脚本见 `styles/style-1~3.md`(配色/字体/装饰 SVG/10 种帧模板/字幕/转场/音轨,构建器按此实现)。

## 四、更换数字人形象

数字人用的是**全局形象库**里的「当前默认形象」,换人不用改代码、不用重启服务:

1. **进入形象页**:任意页面右上角点「**数字人形象**」(地址栏对应 `?avatars=1`);也可以从创作页「数字人出镜」那一栏的「管理形象 / 更换」链接进去
2. **上传**:点上传区选择图片,或直接把图片拖进去 —— 支持 **png / jpg / jpeg / webp**,单张不超过 **20MB**。上传的图会显示分辨率、体积与上传时间;同一张图重复上传不会产生副本
3. **设为默认**:在目标卡片上点「**设为默认**」。卡片会打上「当前默认」角标,顶部一行也会显示当前生效形象
4. **生效时机**:之后**新建**任务,或对已有任务点「构建预览 / 一键出片」**重新构建**,数字人片段就用新形象生成。已经出片的老任务**不会**自动变脸 —— 想换就重新构建一次(形象文件变了就是新的缓存键,旧片段留在缓存里,不会串用别人的画面)
5. **要不要抠背景**:形象页上有一个全局开关「**新任务默认抠掉背景**」,出厂是**开着的** —— 只保留人像、背景透明,幻灯片内容从人像四周透出来(上传的多是真实照片,带背景的卡片会盖住幻灯片)。它只影响**以后新建的任务**;某个任务想单独改,在创作页或预览页「数字人出镜」那一栏勾/去勾「只保留人像(背景透明)」即可(任务自己存过的选择不会被全局开关改掉)
6. **管理**:「**重命名**」只改展示名,不动文件;「**删除**」有二次确认,删掉当前默认形象时默认位会自动落到剩余的形象上。内置的「金立」形象**不可删除**(它是最后的回退),但可以随时设为默认

**配套要点**

- 形象图片与清单存在服务器的 `assets/avatars/`(`library.json`),整目录拷走即可搬家
- 建议用正面、光线均匀、五官清晰的半身照;背景干净能让「只保留人像(背景透明)」的抠像效果更好
- 超过服务端原生输出边长(**464**)的图只会被缩放使用,清晰度不会再增加
- 运维若用 `TTV_AVATAR_IMAGE` 把形象钉死在某个文件上,它的优先级**高于**库设置,页面上会标注「由环境变量指定」——此时在库里改默认不生效(库本身仍然可用,去掉环境变量即恢复)

## 五、运维

### 网页「设置」页(不用改环境变量、不用重启)
顶栏「**设置**」(`/?settings=1`)给不懂服务器的使用者用,只有三块,**保存立刻生效**,不影响已做好的项目:

| 项 | 说明 | 生效方式 |
|---|---|---|
| 文字分析服务 | 地址(如 `https://llmapi.paratera.com/v1`)、密钥、模型名;点「**测试连接**」当场验证(成功显示延迟,失败给人话:密钥错 / 地址写错 / 连不上 / 超时分别提示) | `analyze.py` **每次调用现读**,不用重启 |
| 成片保存位置 | 渲染完成后**额外复制**一份成片过去(项目目录里那份照旧保留,是下载来源);面板显示该磁盘剩余空间;保存时当场校验"能建目录、能写入" | 下一个出片任务生效 |
| 默认设置 | 「新任务默认抠掉数字人背景」(与形象页同一个开关、同一份偏好) | 立刻生效 |

- 值落在 `.run/settings.json`(权限 `0600`);`TTV_DEEPSEEK_URL/_MODEL/_KEY`、`TTV_EXPORT_DIR` 只是**出厂默认**,网页里写过之后以网页为准
- **密钥只写不读**:页面永远不回显明文,只显示 `••••••••末4位`;输入框留空 = 不改动,要清空得点「清除密钥」(二次确认)
- 「恢复默认」= 删掉设置文件,回到环境变量口径(不是把默认值写死进文件,否则以后改环境变量就再也不生效了)
- 换了分析服务之后,**进行中的任务不受影响**,下一次分析用新端点

### 服务与端口
- 后端 FastAPI:`127.0.0.1:8015`(日志 `/mnt/workspace/ttv/logs/backend.log`)
- TTS:CosyVoice3 三实例 **8016(GPU1)/ 8018(GPU2)/ 8019(GPU3)**,后端按帧轮询分发并行合成;Qwen3-TTS 备用 **8017(GPU2)**,默认停
- nginx:20013 端口 `/ttv/` 与 `/ttv/api/` 路由(配置 `/etc/nginx/ttv-locations.conf`,由 comfyui-20013 站点 include;`/ttv/api/` 限流每秒 10 请求、突发 20)
- 每个任务的 Studio 编辑器(hyperframes preview):127.0.0.1:4150–4153(4 槽按需分配,构建期同步启动、按需自愈)
- 并发限制:渲染同时最多 2 个;进行中任务 >4 时新任务返回 429(稍后再试)
- 任务目录:`/mnt/workspace/ttv/jobs/<job_id>/`(含 script.json、project/、渲染产物)
- 日志目录:`/mnt/workspace/ttv/logs/`——`backend.log`(后端)、`tts/tts-8016|8018|8019|qwen.log`(TTS 实例)、`studio/<job_id>.log`(每任务 Studio 输出);项目根目录不再落日志

### 重启后端
```bash
ssh videolab "bash /mnt/workspace/ttv/deploy/start.sh"
```

### BGM 配置(可选)
把三首 MP3 放到 `/mnt/workspace/ttv/assets/bgm/`(文件名固定),下一次构建自动生效:
- `solemn-red.mp3`(庄严管弦)
- `academic-ink.mp3`(古琴/笛箫+弦乐)
- `modern-blue.mp3`(现代管弦+电子脉冲)

缺文件时自动用静音占位,不影响流程。**注意使用有授权的曲目**(建议 CC0/自购/内部素材);若用 CC-BY 曲目(如 Incompetech),请在片尾署名中体现。

### 分析引擎
- 默认:本地 vLLM `http://8.130.213.80:20001/v1`(DeepSeek-V4-Flash,零成本)—— 这是**出厂默认**;网页「设置」页改过之后以网页里的为准
- 备份:云 api.deepseek.com(自动读取 `/mnt/workspace/wsh/cot/.env` 的 DEEPSEEK_API_KEY,本地不可用时回退)

### 配音引擎(CosyVoice3 多实例池)
- **首选**:本地 CosyVoice3-0.5B 三实例 @ 8016(GPU1)/ 8018(GPU2)/ 8019(GPU3),权重 `/mnt/models/CosyVoice3-0.5B`(9.1GB),按帧轮询分发并行合成
  - 三音色:male(云扬新闻男声克隆)/ female(云霞知性女声克隆)/ male_narrator(云健解说腔克隆),种子音频在 `asset-v2/`
  - 重启:`ssh videolab "bash /mnt/workspace/ttv/deploy/start-tts.sh"`(三实例统一拉起,内部已 source PPU SDK 环境)
  - 过短文本(<15 字)服务返回 400 → 流水线**回落静音占位**
- **兜底**:失败重试 ×3 → 静音占位——配音环节永不阻断出片
- **备用引擎**:Qwen3-TTS @ 8017(GPU2,默认停),需要时 `bash deploy/start-tts-qwen.sh` 手动拉起(<8 字返回 400)
- 换音色:替换 `asset-v2/*.wav` 种子音频并同步修改 `tts_server.py` 的 VOICES prompt 文本(需含 `<|endofprompt|>` 前缀)

### 数字人形象库(运维)
- 库目录默认 `assets/avatars/`(图片 + `library.json` 清单),可用 `TTV_AVATAR_LIBRARY` 指向别的目录(既可给目录,也可给目录下的 `library.json`);整目录拷贝即可迁移
- `TTV_AVATAR_IMAGE` 优先级**最高**:设了它,形象就被钉死在这个文件上,页面会标注「由环境变量指定」,库里的默认设置不生效
- `TTV_AVATAR_BUILTIN` 可换内置回退形象的路径(默认 `assets/avatars/jinli.png`;库目录为空/彻底坏掉时回退到它)
- 上传的图片按内容 `sha256` 命名(`av_<hash16>.<ext>`),同一张图不会重复入库;删除条目时对应文件一并清理
- 库的生效是**调用期**解析:换默认形象后,新的构建/出片立即用新形象,不需要重启后端

### 全局偏好(运维)
- 跨任务记住的设置落在 `.run/preferences.json`(可用 `TTV_PREFERENCES` 换路径);目前只有一项 `avatar_cutout_new_jobs`(新任务默认是否抠背景)
- 出厂默认由 `TTV_AVATAR_CUTOUT` 决定(**默认 `1` = 抠**);用户在形象页改过之后,以偏好文件为准,环境变量只作为"文件不存在/读坏时"的兜底
- 文件损坏或字段类型不对会自动回退到出厂默认,不会让接口 500;只影响**没存过几何**的任务,已定好设置的任务不受影响

### 常见问题
- 项目标题不对 / 想改名:入口页卡片或详情页顶部点「重命名」即可(60 字以内)。自动总结的标题在分析完成后出现;手动改过之后再重新分析也不会被覆盖
- 不知道任务跑到哪一步了:入口页卡片上有状态与阶段进度(进行中的任务带转圈图标,列表自动刷新),四个步骤胶囊显示每一步完成的时间;点某一步直达对应结果
- **配音听起来是乱码 / 和字幕对不上**:先看详情页有没有红色提示条(构建时用 whisper 自动抽检过)。有就点旁边的「**重新合成配音**」,它会用当前引擎重烧全部配音并重新构建,之后重新「渲染成片」;没有提示但你自己听出来不对,可以跑一次抽检:
  `tts-venv/bin/python server/voice_check.py --script jobs/<id>/project/script.json --audio jobs/<id>/project/assets/audio --frames 5 --model-dir models/whisper`
  (LCS ≥0.7 正常、≈0 乱码)。乱码的根因与排查路径见 `docs/问题修复说明-配音乱码.md`
- 数字人挡住幻灯片内容:现在**默认就会抠掉背景**(形象页「新任务默认抠掉背景」出厂开着)—— 人像只留本体、幻灯片内容从四周透出来(四个角落照常可选;下排贴画面下缘、上排留头顶空间)。想恢复成带背景的圆角卡片,把那个全局开关关掉(或只对某个任务在「数字人出镜」那一栏去勾「只保留人像(背景透明)」)。抠像按片段缓存(只算一次,CPU 上约 8 帧/秒);抠像解释器或权重缺失时**自动回退**成圆角卡片,不影响出片
- **改了「数字人出镜」的位置/大小/抠像,成片看着没变**:三步确认 —— ① 位置改动只有**重新出片**才生效(点「**一键出片**」就是重跑构建 + 渲染);② 页面可能还在放上一版成片,**刷新页面**(Ctrl/Cmd+Shift+R)再看(成片地址现在带产物时间戳,正常情况下刷新后一定是新版);③ 任务正在构建/渲染时改几何会被挡下(要等这一步跑完),页面会提示但很快消失,别在这期间改
- **换了数字人形象,成片还是原来那张脸**:形象是**构建时**取用的 —— 在「数字人形象」页设为默认之后,要对任务**重新构建**(点「**一键出片**」,或分步「构建预览」→「渲染成片」)才会用新形象;已经出好的成片不会自己变。另外确认没有用 `TTV_AVATAR_IMAGE` 把形象钉死(页面上会标「由环境变量指定」)
- **成片里数字人背景没被抠掉**:三步确认 —— ① 看渲染历史那条有没有「**·抠像**」后缀:没有就是这次构建没开抠像;② 去形象页确认「新任务默认抠掉背景」是否开着(**它只影响以后新建的任务**),这条任务本身则在预览页「数字人出镜」那一栏勾上「只保留人像(背景透明)」;③ 改完必须**重新出片**才生效。抠像落到成片需要重建一遍(片段还在缓存,只补抠像;一段 300×300 的片段 CPU 上约几十秒)
- **上传的形象图被拒绝**:只收 png / jpg / jpeg / webp,单张 ≤20MB,且必须是**能解析出宽高的完整图片**(扩展名改成 `.png` 但内容不是图片会被 400 挡下——这是故意的,损坏的图送到数字人推理会直接失败)。上传同一张图会提示"已经在库里了",不会产生副本
- 渲染慢:1080p 高清渲染为无头 Chrome 逐帧截图,1 分钟成片约 10–25 分钟,属正常(渲染进程会并行 30+ worker;同时最多 2 个任务在渲染)
- **成片做完了,但想直接去文件夹里拿 / 想存到别的盘**:顶栏「**设置**」→「成片保存位置」填一个目录(如 `/mnt/data/成片`)保存即可,**以后**每次出片都会额外复制一份过去(老成片不会补,可在详情页「下载成片」);页面会显示那个位置还剩多少空间。填错/没权限时保存会当场报错,不会等到渲染完才失败
- **换了分析服务(网关/密钥),在哪改**:顶栏「**设置**」→「文字分析服务」,填地址与密钥后点「测试连接」确认能通,再点「保存」—— 立刻生效,不用重启后端,也不影响正在跑的任务
- 预览音频不出声:预览音频依赖浏览器自动播放策略,已注入音频看门狗自动恢复被拦截的死状态(点播放后通常一次即出声)
- Studio 槽位占满(4 个任务同时预览):新任务会回收最旧的 Studio 进程
- 字幕与旁白对不齐:当前为「帧内按字数均分」估算词级时间轴,后续可换 whisper 对齐(服务器已装 openai-whisper)
- 服务重启:后端 `bash deploy/start.sh`;TTS `bash deploy/start-tts.sh`;重启后任务自动恢复,进行中任务(uploaded/analyzing/building/rendering)标记 failed 可重新分析,preview 任务自动重建 Studio

## 六、架构

```
浏览器 ─▶ nginx(20013/ttv) ─┬─ 静态页(web/index.html,字体 woff2 子集经 /ttv/assets/ 加载)
                             └─ /api → FastAPI(8015)
                                        ├─ extract:txt/md/docx 提取
                                        ├─ analyze:DeepSeek 分析(本地 vLLM 优先;宣传两阶段/讲解备课+分段并行)
                                        ├─ build:CosyVoice3 三实例(8016/8018/8019)并行配音 + builder 组装 composition
                                        ├─ render:hyperframes CLI(无头 Chrome 逐帧渲染,并发 ≤2)
                                        └─ studio:每任务 hyperframes preview 进程(4150–4153 槽位,反向代理)
```

与「服务器上跑 Claude Code/Codex agent + skills」路线的关系:两者共用同一 vLLM、同一 hyperframes skills 与渲染 CLI,差异仅在「脚本生成」环节——本系统把 skill 规则固化成了确定性模板管线(受控 LLM 调用 + 模板构建 + 校验修复),agent 路线是每次派 agent 现场创作(更灵活但更慢、更贵、并发差)。如未来需要,可在「analyze」环节加挂 Codex 类 agent 替换,其余不动。
