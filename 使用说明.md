# 理论文章转视频 · 使用说明

> 站点地址:**http://8.130.213.80:20013/ttv/**(外网直接访问,无需登录)
> 服务器部署目录:`/mnt/workspace/ttv/`

## 一、使用流程

1. **上传文章**(txt / md / docx,≤20MB)或**粘贴文字**(≥50 字)→ 选风格(三套预设或四维度自由组合)→ 选**视频类型**(宣传/讲解,共用一个时长滑杆)→ 拖时长(宣传 30 秒 – 10 分钟;讲解 5 – 30 分钟,每 5 分钟一档)→「开始分析」
2. 服务器调用本地 DeepSeek-V4-Flash(vLLM @20001)分析:
   - **宣传视频**:一次分析,产出大纲分析、视频结构、逐帧脚本、风格推荐
   - **讲解视频**:两步分析——先诊断文章类型与讲解方案(章节安排、逐段讲解要点、待批注句、可迁移写法、答题方法总结),再按章节分段生成逐帧脚本(耗时 3–10 分钟,页面显示阶段进度)
3. 二级页面查看**分析结果**(讲解视频含完整备课方案)与**逐帧脚本表**(可手动编辑或一句话让 AI 修改)→ 点「构建预览」
4. 构建期间自动完成:配音合成(CosyVoice3 本地,自然语速)→ 旁白量不足目标时**自动二次拓展内容** → 组装 HyperFrames 项目 → 预览就绪时 **Studio 编辑器已同步启动**,页面直接进入编辑器(时间线、逐帧编辑、实时预览)
5. 满意后点「渲染成片」(约 5–20 分钟,视时长;30 分钟讲解片渲染更久,超时上限 3 小时),完成后同一位置切换为成片播放,可在线观看 + 下载 MP4

**时长滑杆原理**:选择时长 → 分析引擎按档位规划帧数与旁白量(语速按 4.2 字/秒实测换算,配音始终自然语速 1.0)→ 构建时按真实配音时长向目标靠拢。**时长靠内容充实度填**:每帧旁白 2-4 句、画面元素填满上限,不靠降速或拖延。

## 二、视频类型

### 宣传视频(默认)
提炼主旨、归档宣传:把文章改写为庄重的政论宣传片(开场钩子 → 核心论点 → 是什么-为什么-怎么办 → 收束署名)。

### 讲解视频
**老师逐段精讲**,像讲作文一样拆解文章(申论讲解/政论解读式授课),不是把文章念一遍:

- 旁白为老师口吻:先点出这段在干什么、为什么这么写,再带学生看关键句;明确区分「文章认为……」与「目前已知事实是……」
- 三种讲解专用画面:
  - **原文段页(textblock)**:讲义式展示原文段落 + 本段作用标签
  - **逐句批注页(annotation)**:原句逐字展示 + 论点/论据/分析/对策/过渡/金句彩色标注 + 老师批注
  - **写法提炼页(method)**:把这段写法抽象成可迁移的板书卡片
- 复用宣传帧:statement(设问引导)/points/process(文章框架图)/contrast(原文观点 vs 已知事实)/quote(金句赏析)/data
- **原文引用逐字校验**:textblock/annotation 引用的原句必须逐字摘自原文(允许截断),校验门硬拦截改写与编造
- 时长 5–30 分钟,每 5 分钟一档;复用同一套四维风格系统

## 三、三套风格

| 键 | 名称 | 适用 |
|---|---|---|
| solemn-red | 庄重肃穆·中国红 | 人民日报评论、时政理论 |
| academic-ink | 清雅学术·墨黛青 | 马院论文、学术理论 |
| modern-blue | 现代锐意·科技蓝 | 改革、科技、新质生产力 |

详细样式脚本见 `styles/style-1~3.md`(配色/字体/装饰 SVG/10 种帧模板/字幕/转场/音轨,构建器按此实现)。

## 四、运维

### 服务与端口
- 后端 FastAPI:`127.0.0.1:8015`(日志 `/mnt/workspace/ttv/backend.log`)
- nginx:20013 端口 `/ttv/` 与 `/ttv/api/` 路由(配置 `/etc/nginx/ttv-locations.conf`,由 comfyui-20013 站点 include)
- 每个任务的 Studio 编辑器(hyperframes preview):127.0.0.1:4150–4153(4 槽按需分配,构建期同步启动、按需自愈)
- 任务目录:`/mnt/workspace/ttv/jobs/<job_id>/`(含 script.json、project/、渲染产物)

### 重启后端
```bash
ssh videolab "bash /mnt/workspace/ttv/deploy/start.sh"
```

### BGM 配置(可选)
把三首 MP3 放到 `/mnt/workspace/ttv/assets/bgm/`(文件名固定),下一次构建自动生效:
- `solemn-red.mp3`(庄严管弦)
- `academic-ink.mp3`(古琴/笛箫+弦乐)
- `modern-blue.mp3`(现代管弦+电子脉冲)

缺文件时自动用静音占位,不影响流程。**注意使用有授权的曲目**(建议 CC0/自购/内部素材);若用 CC-BY 曲目(如 Incompetech),请在片尾署名中体现。

### 分析引擎
- 默认:本地 vLLM `http://8.130.213.80:20001/v1`(DeepSeek-V4-Flash,零成本)
- 备份:云 api.deepseek.com(自动读取 `/mnt/workspace/wsh/cot/.env` 的 DEEPSEEK_API_KEY,本地不可用时回退)

### 配音引擎(CosyVoice3)
- **首选**:本地 CosyVoice3-0.5B @ 127.0.0.1:8016(GPU1),权重 `/mnt/models/CosyVoice3-0.5B`(9.1GB)
  - 三音色:male(云扬新闻男声克隆)/ female(云霞知性女声克隆)/ male_narrator(云健解说腔克隆),种子音频在 `asset-v2/`
  - 重启:`ssh videolab "bash /mnt/workspace/ttv/deploy/start-tts.sh"`(内部已 source PPU SDK 环境)
  - 过短文本(<15 字)服务返回 400 → 流水线自动回落 edge-tts
- **兜底**:edge-tts(重试+换声线),再不行静音占位——配音环节永不阻断出片
- 换音色:替换 `asset-v2/*.wav` 种子音频并同步修改 `tts_server.py` 的 VOICES prompt 文本(需含 `<|endofprompt|>` 前缀)

### 常见问题
- 渲染慢:1080p 高清渲染为无头 Chrome 逐帧截图,1 分钟成片约 10–25 分钟,属正常(渲染进程会并行 30+ worker)
- 预览播放器默认静音:播放器界面自带控制条,点一下取消静音即可(play 页面默认 muted,属 hyperframes 行为)
- 播放器端口占用满(6 个任务同时预览):新任务会回收最旧的预览进程
- 字幕与旁白对不齐:当前为「帧内按字数均分」估算词级时间轴,v2 可换 whisper 对齐(服务器已装 openai-whisper)
- 服务重启:后端 `bash deploy/start.sh`;TTS `bash deploy/start-tts.sh`;重启后任务自动恢复,preview 任务自动重建播放器

## 五、架构

```
浏览器 ─▶ nginx(20013/ttv) ─┬─ 静态页(web/index.html)
                             └─ /api → FastAPI(8015)
                                        ├─ extract:txt/md/docx 提取
                                        ├─ analyze:DeepSeek 分析(本地 vLLM 优先)
                                        ├─ build:edge-tts 配音 + builder 组装 composition
                                        ├─ render:hyperframes CLI(无头 Chrome 逐帧渲染)
                                        └─ player:每任务一个 hyperframes play 进程(反向代理)
```

与「服务器上跑 Claude Code/Codex agent + skills」路线的关系:两者共用同一 vLLM、同一 hyperframes skills 与渲染 CLI,差异仅在「脚本生成」环节——本系统把 skill 规则固化成了确定性模板管线(一次 LLM 调用+模板构建),agent 路线是每次派 agent 现场创作(更灵活但更慢、更贵、并发差)。如未来需要,可在「analyze」环节加挂 Codex 类 agent 替换,其余不动。
