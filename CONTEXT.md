# article2video

把一篇文章自动转换成"PPT 式"视频：LLM 生成解说脚本，逐帧配音，浏览器渲染成片。

## Language

**帧（frame）**：
成片的一个幻灯片单元，承载一段台词与一组画面内容；时间轴、字幕、数字人片段都以帧为单位对齐。
_Avoid_: 页、幻灯片、slide

**台词（voiceover）**：
每帧被朗读的文本，字段是 `frames[].voiceover`。它是**唯一来源**：同一份文本既被合成为该帧的配音，又被渲染成底部字幕，也是数字人朗读的内容。
_Avoid_: 旁白、讲稿、字幕文本（把它们当成三个不同的东西）

**字幕窗口（caption window）**：
台词按每行不超过 19 字切分后，随时间逐窗口显示的底部字幕单元，一个窗口 1–2 行。
_Avoid_: 字幕行（一行与一个窗口不是一回事）

**脚本（script）**：
一次任务的完整帧序列与元数据（`script.json`），由分析阶段产出、经配音阶段用真实音频时长回填。
_Avoid_: 大纲、方案

**任务（job）**：
一次"文章 → 成片"的完整生成单元，有独立的目录与状态机。
_Avoid_: 项目（"项目"在本仓库里另有所指，见下）

**项目目录（project）**：
单个任务内部的渲染工程目录，装着 `index.html`、`hyperframes.json`、音频与产物。
_Avoid_: 任务（与用户视角的"任务"混用会造成歧义）

**项目标题（project title）**：
入口页项目列表里那条视频的显示名（一个任务一个），**不是**脚本标题（`script.title` 是片内标题）。来源记在 `state.title_source`：`file`（建任务时取原文件名）、`auto`（分析完成后大模型总结的短标题）、`user`（用户在列表或详情页改的）；一旦是 `user`，自动总结不再覆盖。
_Avoid_: 脚本标题、视频标题（把两个字段当成一个）

**项目历史（project history）**：
一个任务里每个流水线阶段（分析脚本 / 构建预览 / 渲染成片 / 播报视频）**完成或失败**留下的一条记录（`state.history`，含时间与结果摘要），是"每一步出了什么"的账本。它**不保存每次渲染的成片副本**——当前产物始终以 `state.artifacts` 为准。
_Avoid_: 版本历史（没有多个成片版本）、日志（`logs/` 是进程输出，另一回事）

**数字人片段（avatar clip）**：
单帧对应的独立短视频，由**该帧台词音频**驱动数字人嘴型，只提供画面（自带音轨丢弃），画面为**纯净的 `size`×`size`**（不带圆角、不带投影、不带背景透明）；圆角与投影在叠加时完成，不在片段里。抠像模式下它旁边会多一条同名的灰度**抠像遮罩**（见后文），两者一起缓存。
_Avoid_: 数字人视频、头像视频

**数字人时间轴（avatar timeline）**：
把每帧数字人片段与**该帧在成片里的绝对起点**对应起来的一份清单，由 `assemble.build()` 的 `starts` 生成，是叠加步骤的唯一依据。
_Avoid_: 字幕时间轴（那是词级时间，另一回事）

**待机片段（idle clip）**：
数字人闭嘴静默的短视频。每个形象只生成一段、全局缓存，用于填充帧内**没有台词**的时间（尾部留白，以及 opening/closing 这类无台词帧）。
_Avoid_: 空闲视频、静止片段

**尾部留白（tail pad）**：
帧时长超出该帧配音时长的部分，用于句间停顿。帧时长 = 台词配音实际时长 + 尾部留白。
_Avoid_: padding、间隔、gap

**成片（final render）**：
浏览器渲染出的 PPT 视频与各帧数字人片段叠加后的最终输出文件。
_Avoid_: 成品视频、输出

**数字人播报视频（broadcast video）**：
把各帧数字人片段按时间轴**顺序拼接**、并合上同一份配音音轨的独立视频（正方形，含声音，不含 PPT 画面与 BGM）。它是分析完成后由独立动作触发的一条支线，不经过 HyperFrames 渲染，也不依赖成片。
_Avoid_: 数字人视频（与「数字人片段」混用）、口播片、数字人成片

**数字人大小（avatar size）**：
数字人卡片的边长（正方形，故一个数即尺寸）。默认 300（`config.AVATAR_SIZE`），**成片叠加与播报视频都按任务改**，页面上是手输数字（档位 240/300/400/464/640 只作提示）。必须偶数，范围 160–1080；超过 464（服务端原生输出）只是放大，清晰度不再增加。任务的成片几何记在 `state.avatar_geom.size`，播报视频另有 `state.avatar_broadcast_size`（没单独设过就跟随前者）；每条播报视频**实际**用的尺寸记在 `avatar_broadcast.size`，与输入框里**当前填着**的数字是两件事。
_Avoid_: 分辨率（那是成片 1920×1080 的说法）、缩放比例

**数字人出镜（avatar on/off）**：
成片里要不要出现那个人像。**「不出镜」是一个和四个角落并列的选项**（同一下拉里的第一项），不是一个额外的勾选框——用户看到的那一栏就是"出镜 + 出在哪"。它跟任务走（`state.avatar`，默认 false＝不出镜），创作页建任务时选，预览页随时可改（包括从出镜切成不出镜）。**关掉之后重新渲染必须是纯 PPT 版**：哪怕上一次构建留下的 `avatar_timeline.json` 还在磁盘上，也不能再把片段叠回去（见 `_overlay_timeline`）。
_Avoid_: 关闭/删除数字人（片段与缓存都留着，随时能再打开）、把不出镜混进「数字人位置」的取值里

**数字人位置（avatar corner）**：
成片里那个人像落在哪个角落：`tl` 左上 / `tr` 右上 / `br` 右下 / `bl` 左下，**默认右上**（`config.AVATAR_CORNER`）。只影响成片叠加——播报视频里画面就是人像本身，没有"位置"可言。跟着任务走（`state.avatar_geom.corner`），创作页建任务时设置，预览页随时可改，重新构建渲染后生效。**"不出镜"不属于位置**：它是上一条的出镜开关，位置只是"出镜时出在哪"。**抠像时上下角等价**（都贴画面下缘），只有左右还有意义。
_Avoid_: 坐标（`AVATAR_X/AVATAR_Y` 是距边的留白，不是位置本身）、对齐

**抠像出镜（avatar cutout）**：
出镜时把数字人的**背景抠掉、只叠人像**（界面上是"只保留人像（背景透明）"），幻灯片内容从人像四周透出来，不再被那块不透明卡片压住。跟任务走（`state.avatar_geom.cutout`，默认 false），创作页与预览页都能开。**人像贴画面下缘**：片段是齐胸特写、底边整行都是躯干，只有让那道平切口落在画面外沿，看上去才不是"悬浮的半身像"；因此抠像时上下角等价（`tl≡bl`、`tr≡br`），只有左右由位置决定。抠像**只作用于成片叠加**，播报视频照旧是不透明的独立人像片。抠像不可用（解释器/权重缺失）时**逐条回退**成圆角卡片，成片照出。
_Avoid_: 透明视频（成片仍是 H.264，没有 alpha 通道；透明发生在叠加那一步）、绿幕、把抠像当成第五个角落

**抠像遮罩（matte）**：
每段数字人片段旁边那条**灰度**视频（`clip_*.modnet<版本>.mp4`），白＝人像、黑＝背景，由 `server/matte.py`（MODNet ONNX）逐帧算出，与片段同尺寸、同帧率、同时长。它是**独立文件**而不是片段的 alpha 通道——H.264 不支持 alpha（见 ADR-0002），而灰度遮罩体积很小、还能单独失效。叠加时 `[片段][遮罩]alphamerge` 合成，取的是遮罩的**亮度**，所以遮罩必须是灰度。
_Avoid_: 蒙版（那说的是圆角卡片那张几何遮罩 `mask_*.png`）、alpha 片段、形状遮罩

**播报三步（broadcast steps）**：
生成数字人播报视频的三个步骤——合成配音 → 生成数字人片段 → 拼接播报视频。界面上要按这三步展示进度与预计剩余时间。
_Avoid_: 渲染阶段（渲染专指 HyperFrames 出片）

